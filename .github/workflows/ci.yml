name: CI

on:
  push:
    branches: [main, "refactor/**", "feature/**"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.9", "3.11", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Installa il progetto
        run: python -m pip install --upgrade pip && pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check .

      - name: Test
        run: python -m pytest -q

      # I test non richiedono un dispositivo: gli strumenti esterni sono
      # invocati solo dai percorsi di codice che toccano l'hardware.
      - name: Verifica che la CLI si avvii
        run: |
          python -m iosforensic --version
          python -m iosforensic doctor || true
